package com.saltclient.module;

/**
 * A placeholder module with no functionality yet.
 * Used to keep the "module list" complete while we implement features over time.
 */
public final class SimpleModule extends Module {
    public SimpleModule(String id, String name, String description, ModuleCategory category) {
        super(id, name, description, category, false);
    }
}

